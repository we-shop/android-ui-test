[pytest]
markers =
    uat: mark to test on uat.
    int: mark to test on int.

addopts = --driver BrowserStack --capability browserName Appium -n=2 -v -s --html=report.html --self-contained-html --disable-pytest-warnings